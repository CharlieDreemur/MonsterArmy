using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//一个ScriptableObject存储当前场景的敌人List，友方List等战斗数据(未启用，先用monobehavior凑合下)
[CreateAssetMenu(fileName = "ManagerData", menuName = "TheMonsterArmy/ScriptableObject/ManagerData", order = 0)]
public class ManagerData : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
